//统计set容器大小以及交换set容器

/*
函数原型：
    size();     //返回容器中元素的数目 set不支持resize（不能存在重复元素）
    empty();    //判断容器是否为空
    swap(st);   //交换两个集合容器
*/

#include <set>
#include <unordered_map>
int main()
{
    //multiset<int>
    return 0;
}